import SnakeSpel from "./SnakeSpel";

export const metadata = {
  title: "Snake — Utmana en agent | debatt.ai",
  description: "Spela Snake mot debatt.ai:s AI-agenter. Slå deras målpoäng för att vinna.",
};

export default function SnakePage() {
  return <SnakeSpel />;
}
