"use client"; export const dynamic = 'force-dynamic'; import { useState, useEffect } from "react"; const MIN_SCORE = 6; const SB_URL = process.env.NEXT_PUBLIC_SUPABASE_URL; const SB_KEY = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; const SYSTEM_PROMPT = `Du är chefredaktör för en svensk debattajts. Bedöm artikeln på fyra kriterier (heltal 0-10): 1. Argumentationsklarhet – Är argumenten tydliga och logiskt uppbyggda? 2. Originalitet – Tillför artikeln något nytt till debatten? 3. Samhällsrelevans – Är ämnet viktigt och aktuellt? 4. Trovärdighet – Är faktapåståendena rimliga och välgrundade? En artikel kan publiceras om ALLA fyra poäng är minst ${MIN_SCORE}/10. Svara ENDAST med JSON (inga andra tecken): {"beslut":"publicera","motivering":"kort motivering","arg":8,"ori":7,"rel":9,"tro":8,"forbattringar":["förslag 1","förslag 2"],"styrkor":["styrka 1"],"rubrik":null} beslut är "publicera" om alla fyra >= ${MIN_SCORE}, annars "revidera" eller "avvisa".`; const EXAMPLE = `Sverige har en självbild som ett teknologiskt framstående land. Vi är stolta över vår digitalisering, vår välfärd och vår förmåga att anpassa oss. Men just nu blundar vi kollektivt för den mest omvälvande ekonomiska förändringen sedan industrialiseringen. Automatiseringen är inte längre en framtidsfråga. Den pågår just nu, i tysta serverhallar. Goldman Sachs beräknar att generativ AI kan automatisera upp till 300 miljoner heltidstjänster globalt. I Sverige visar SCB:s yrkesregister att drygt 800 000 tjänstemän arbetar med uppgifter som till mer än hälften består av text och databearbetning. Det handlar inte om lågkvalificerade jobb. Det handlar om medelklassens kärna – de utbildade, välbetalda positioner som en generation av unga har investerat studielån och tio år av karriär i. Politiken sover. Ingen av riksdagspartierna presenterar en trovärdig plan för vad som händer när strukturell övertalighet drabbar breda yrkesgrupper simultant. Lösningen är inte att bromsa AI-utvecklingen. Det är lika meningslöst som att försöka stoppa elektricitet. Precis som elektrifieringen krävde ny lagstiftning och nya socialförsäkringar, kräver AI-omvälvningen nya fördelningsmekanismer. Norge har gjort det med oljan. Vi kan göra det med algoritmerna.`; // ── Supabase REST helpers ───────────────────────────────────────────────────── function sbHeaders() { return { "apikey": SB_KEY, "Authorization": `Bearer ${SB_KEY}`, "Content-Type": "application/json", "Prefer": "return=minimal", }; } async function sbInsert(row) { const res = await fetch(`${SB_URL}/rest/v1/artiklar`, { method: "POST", headers: sbHeaders(), body: JSON.stringify(row), }); if (!res.ok) throw new Error(await res.text()); } async function sbSelect() { const res = await fetch(`${SB_URL}/rest/v1/artiklar?select=*&order=skapad.desc`, { headers: { "apikey": SB_KEY, "Authorization": `Bearer ${SB_KEY}` }, }); if (!res.ok) throw new Error(await res.text()); return res.json(); } // ── Styles ──────────────────────────────────────────────────────────────────── const C = { bg: "#0a0a0a", surface: "#111111", border: "#222222", accent: "#e8d5a3", accentDim: "#b8a57a", text: "#f0ede6", textMuted: "#888880", green: "#4ade80", red: "#f87171", yellow: "#fbbf24", }; const inp = { background: "#0d0d0d", border: `1px solid ${C.border}`, borderRadius: "4px", color: C.text, fontFamily: "Georgia, serif", fontSize: "15px", padding: "12px 14px", width: "100%", boxSizing: "border-box", outline: "none", lineHeight: "1.5", }; // ── Components ──────────────────────────────────────────────────────────────── function Lbl({ children }) { return ( <label style={{ display: "block", fontSize: "11px", color: C.textMuted, letterSpacing: "0.1em", textTransform: "uppercase", marginBottom: "6px" }}> {children} </label> ); } function ScoreBar({ label, value }) {  
